class Solution(object):
    def containsDuplicate(self, nums):
        x = list((set(nums)))
        x = len(x)
        y = len(nums)
        if (x == y):
            return False
        else:
            return True
